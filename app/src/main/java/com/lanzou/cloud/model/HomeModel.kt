package com.lanzou.cloud.model

import androidx.databinding.BaseObservable

class HomeModel : BaseObservable() {

  /* @get:Bindable
   var currentFocusedPosition: LayoutPosition = LayoutPosition.LEFT
     set(value) {
       field = value
       notifyPropertyChanged(BR.currentFocusedPosition)
     }*/
}